# =============================================================================
# General Configuration
# =============================================================================

# System Settings
PUID=1000
PGID=1000
TZ=America/New_York

# =============================================================================
# Torrent Client Selection
# Uncomment ONE of the clients in docker-compose.yml and set TORRENT_CLIENT
# =============================================================================

# Which client to use (rtorrent | qbittorrent | transmission | deluge | aria2)
TORRENT_CLIENT=rtorrent

# -----------------------------------------------------------------------------
# rTorrent Configuration (ruTorrent Web UI)
# Default: Enabled in docker-compose.yml
# Web UI: http://localhost (via nginx)
# -----------------------------------------------------------------------------
RTORRENT_USER=admin
RTORRENT_PASSWORD=password
TORRENT_URL=http://vpn:8000/plugins/httprpc/action.php

# -----------------------------------------------------------------------------
# qBittorrent Configuration (Web UI)
# To use: Uncomment 'qbittorrent' service in docker-compose.yml
# Web UI: http://localhost:8080
# Default credentials: admin / adminpass (change on first login)
# -----------------------------------------------------------------------------
# TORRENT_USER=admin
# TORRENT_PASSWORD=adminpass
# TORRENT_URL=http://vpn:8080

# -----------------------------------------------------------------------------
# Transmission Configuration (Web UI)
# To use: Uncomment 'transmission' service in docker-compose.yml
# Web UI: http://localhost:9091
# Default credentials: transmission / transmission
# -----------------------------------------------------------------------------
# TORRENT_USER=transmission
# TORRENT_PASSWORD=transmission
# TORRENT_URL=http://vpn:9091/transmission/rpc

# -----------------------------------------------------------------------------
# Deluge Configuration (Web UI)
# To use: Uncomment 'deluge' service in docker-compose.yml
# Web UI: http://localhost:8112
# Default password: deluge (for Web UI only, not TORRENT_USER)
# -----------------------------------------------------------------------------
# TORRENT_PASSWORD=deluge
# TORRENT_URL=http://vpn:8112

# -----------------------------------------------------------------------------
# aria2 Configuration (JSON-RPC)
# To use: Uncomment 'aria2' service in docker-compose.yml
# Web UI: http://localhost:6800 (or use AriaNg)
# RPC Secret: Set RPC_SECRET for authentication
# -----------------------------------------------------------------------------
# RPC_SECRET=my_secret_token_here
# LISTEN_PORT=6888
# TORRENT_URL=http://vpn:6800/jsonrpc

# =============================================================================
# VPN Provider Settings
# Change VPN_SERVICE_PROVIDER to match your chosen provider
# See /docs/[provider]-setup.md for provider-specific instructions
# =============================================================================

VPN_SERVICE_PROVIDER=nordvpn
VPN_TYPE=wireguard

# Server Selection (optional - customize for your needs)
SERVER_COUNTRIES=United States
SERVER_CATEGORIES=P2P

# Network & DNS Configuration
FIREWALL_OUTBOUND_SUBNETS=192.168.1.0/24
DNS_SERVER=on
DNS_ADDRESS=127.0.0.1
DNS_UPSTREAM_RESOLVER_TYPE=plain
DNS_UPSTREAM_RESOLVERS=cloudflare
DNS_UPSTREAM_IPV6=off
DNS_CACHING=on

# =============================================================================
# VPN Credentials - Choose ONE provider below and uncomment its section
# See /docs/[provider]-setup.md for detailed credential instructions
# =============================================================================

# -----------------------------------------------------------------------------
# NordVPN (WireGuard) - DEFAULT CONFIGURATION
# Get token: https://my.nordaccount.com/dashboard/nordvpn/access-tokens
# -----------------------------------------------------------------------------
NORDVPN_TOKEN=your_nordvpn_token_here
WIREGUARD_PRIVATE_KEY=your_wireguard_private_key_here

# -----------------------------------------------------------------------------
# Mullvad (WireGuard)
# Get key: https://mullvad.net/account/ -> WireGuard configuration
# If using Mullvad, change VPN_SERVICE_PROVIDER to: mullvad
# -----------------------------------------------------------------------------
# WIREGUARD_PRIVATE_KEY=your_mullvad_private_key_here
# WIREGUARD_ADDRESSES=10.64.0.1/32

# -----------------------------------------------------------------------------
# Private Internet Access (OpenVPN)
# Use your PIA username and password
# If using PIA, change VPN_SERVICE_PROVIDER to: private internet access
# If using PIA, change VPN_TYPE to: openvpn
# -----------------------------------------------------------------------------
# OPENVPN_USER=your_pia_username
# OPENVPN_PASSWORD=your_pia_password

# -----------------------------------------------------------------------------
# ProtonVPN (OpenVPN)
# Get credentials: https://account.protonvpn.com/account#openvpn
# If using ProtonVPN, change VPN_SERVICE_PROVIDER to: protonvpn
# If using ProtonVPN, change VPN_TYPE to: openvpn
# -----------------------------------------------------------------------------
# OPENVPN_USER=your_protonvpn_openvpn_username
# OPENVPN_PASSWORD=your_protonvpn_openvpn_password

# -----------------------------------------------------------------------------
# ExpressVPN (OpenVPN)
# Get activation code from ExpressVPN dashboard
# If using ExpressVPN, change VPN_SERVICE_PROVIDER to: expressvpn
# If using ExpressVPN, change VPN_TYPE to: openvpn
# -----------------------------------------------------------------------------
# EXPRESSVPN_ACTIVATION_CODE=your_activation_code_here

# -----------------------------------------------------------------------------
# Surfshark (WireGuard/OpenVPN)
# Use your Surfshark credentials
# If using Surfshark, change VPN_SERVICE_PROVIDER to: surfshark
# -----------------------------------------------------------------------------
# SURFSHARK_USER=your_surfshark_username
# SURFSHARK_PASSWORD=your_surfshark_password

# -----------------------------------------------------------------------------
# Other Providers
# See /docs/ directory for setup instructions for all 23 supported providers:
# - AirVPN, Cyberghost, FastestVPN, HideMyAss, IPVanish, IVPN
# - Perfect Privacy, PrivateVPN, Privado, PureVPN, SlickVPN
# - TorGuard, VPN Secure, VPN Unlimited, VyprVPN, Windscribe
# - Custom (bring your own config)
#
# For each provider:
# 1. Change VPN_SERVICE_PROVIDER to the provider name
# 2. Change VPN_TYPE if needed (wireguard or openvpn)
# 3. Uncomment and fill in the provider's credential variables
# 4. See /docs/[provider]-setup.md for exact variable names and setup
# -----------------------------------------------------------------------------
